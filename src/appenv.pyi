"""Type stubs for appenv."""

import argparse
from pathlib import Path
from typing import Any

__version__: str

PYPROJECT_TOML: str
UV_LOCK: str

# Exit codes (BSD sysexits.h conventions)
EXIT_CODE_DATAERR: int  # Input data issue (Python version not found)
EXIT_CODE_NOINPUT: int  # Missing input file (pyproject.toml/uv.lock not found)
EXIT_CODE_UNAVAILABLE: int  # Resource unavailable (uv too old)

_uv_bin_cache: str | None

def cmd(
    c: str | list[str],
    *,
    merge_stderr: bool = True,
    quiet: bool = False,
    cwd: str | None = None,
) -> bytes: ...
def has_nix() -> bool: ...
def verbose_print(*args: Any, **kwargs: Any) -> None: ...
def print_colored_diff(
    old_content: str, new_content: str, fromfile: str, tofile: str
) -> bool: ...
def get_uv_bin(base: Path | None = None) -> str: ...
def ensure_uv(base: Path) -> None: ...
def python(
    path: Path, c: list[str], *, merge_stderr: bool = True, quiet: bool = False
) -> bytes: ...
def uv_cmd(
    args: list[str],
    verbose: bool = False,
    **kwargs: Any,
) -> bytes: ...
def ensure_venv(target: Path, base: Path | None = None) -> None: ...
def ensure_best_python(base: Path) -> None: ...
def detect_project_type(base: Path) -> str | None: ...
def parse_requires_python(pyproject_path: Path) -> tuple[str | None, str | None]: ...
def find_available_pythons() -> list[tuple[str, str]]: ...
def version_satisfies_constraints(
    version: str, min_version: str, max_version: str | None = None
) -> bool: ...
def parse_uv_version(version_str: str) -> tuple[int, int, int]: ...
def check_uv_version() -> tuple[int, int, int]: ...
def ensure_uv_version() -> None: ...
def parse_editable_spec(spec: str) -> dict[str, Any] | None: ...
def extract_package_name_from_path(path: str, base_dir: Path) -> str | None: ...
def main() -> None: ...

class AppEnv:
    base: Path
    appenv_dir: Path
    original_cwd: Path

    def __init__(self, base: Path, original_cwd: Path) -> None: ...
    def meta(self) -> None: ...
    def run(self, command: str, argv: list[str]) -> None: ...
    def prepare(
        self,
        args: argparse.Namespace | None = None,
        remaining: list[str] | None = None,
    ) -> str: ...
    def init(
        self,
        args: argparse.Namespace | None = None,
        remaining: list[str] | None = None,
    ) -> None: ...
    def migrate(
        self,
        args: argparse.Namespace | None = None,
        remaining: list[str] | None = None,
    ) -> None: ...
    def _create_pyproject(
        self,
        target: Path,
        project_name: str,
        description: str,
        dependencies: list[str],
        editable_sources: dict[str, Any],
        python_version: str,
        command_name: str | None,
    ) -> None: ...
    def python(self, args: argparse.Namespace, remaining: list[str]) -> None: ...
    def run_script(self, args: argparse.Namespace, remaining: list[str]) -> None: ...
    def show_version(
        self,
        args: argparse.Namespace | None = None,
        remaining: list[str] | None = None,
    ) -> None: ...
    def reset(
        self,
        args: argparse.Namespace | None = None,
        remaining: list[str] | None = None,
    ) -> None: ...
    def update_lockfile(
        self,
        args: argparse.Namespace | None = None,
        remaining: list[str] | None = None,
    ) -> None: ...
    def _prepare_pyproject(self) -> str: ...
    def _update_lockfile_pyproject(
        self, args: argparse.Namespace | None, verbose: bool = False
    ) -> None: ...
