"""Type stubs for appenv."""

import argparse
from typing import BinaryIO

class TColors:
    """Terminal colors for pretty output."""

    RED: str
    GREEN: str
    YELLOW: str
    RESET: str

def cmd(
    c: str | list[str], *, merge_stderr: bool = True, quiet: bool = False
) -> bytes: ...
def get(host: str, path: str, f: BinaryIO) -> None: ...
def has_uv() -> bool: ...
def python(
    path: str, c: list[str], *, merge_stderr: bool = True, quiet: bool = False
) -> bytes: ...
def pip(
    path: str, c: list[str], *, merge_stderr: bool = True, quiet: bool = False
) -> bytes: ...
def uv_cmd(
    args: list[str], *, merge_stderr: bool = True, quiet: bool = False
) -> bytes: ...
def parse_preferences() -> list[str] | None: ...
def ensure_venv(target: str) -> None: ...
def ensure_minimal_python() -> None: ...
def ensure_best_python(base: str) -> None: ...
def main() -> None: ...

class AppEnv:
    base: str
    appenv_dir: str
    original_cwd: str

    def __init__(self, base: str, original_cwd: str) -> None: ...
    def meta(self) -> None: ...
    def run(self, command: str, argv: list[str]) -> None: ...
    def prepare(
        self,
        args: argparse.Namespace | None = None,
        remaining: list[str] | None = None,
    ) -> str: ...
    def init(
        self,
        args: argparse.Namespace | None = None,
        remaining: list[str] | None = None,
    ) -> None: ...
    def python(self, args: argparse.Namespace, remaining: list[str]) -> None: ...
    def run_script(self, args: argparse.Namespace, remaining: list[str]) -> None: ...
    def reset(
        self,
        args: argparse.Namespace | None = None,
        remaining: list[str] | None = None,
    ) -> None: ...
    def update_lockfile(
        self,
        args: argparse.Namespace | None = None,
        remaining: list[str] | None = None,
    ) -> None: ...
    def _assert_requirements_lock(self) -> None: ...
    def _hash_requirements(self) -> str: ...
